stages:
  - build

build:
    stage: build
    image: kitware/cmake:ci-debian12-x86_64-2025-03-31

    script:
      - mkdir build
      - cd build
      - cmake ..
      - cmake --build .
    artifacts:
      paths:
      - build/
      expire_in: 1 hour
    tags: 
      - student-code